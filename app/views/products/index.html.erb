<section id="list-paintings">
    <h2>Paintings For Sale</h2>
    <ul>
        <% @products.each do |p| %>
        <li><%= link_to p.name, product_path(p) %></li>
        <% end %>
    </ul>
</section>